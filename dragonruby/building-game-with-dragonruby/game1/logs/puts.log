* INFO - If you need to find rogue puts statements do:
#+begin_src
  def tick args
    # adding the following line to the TOP of your tick method
    # will print ~caller~ along side each ~puts~ statement
    $gtk.trace_puts!
  end
#+end_src
-> reset
* EXCEPTION: * ERROR - :reset method missing on ~Class~.

#+begin_src ~Class#inspect~
GTK::ConsoleEvaluator
#+end_src ~Class#inspect~



The method named
  :reset
doesn't exist on [Class].

(suggestions above)
-> :reset
=> :reset
-> gtk:reset
* EXCEPTION: line 12: syntax error, unexpected label, expecting keyword_end
** Backtrace:
